# Wrapper ERC20 Token project using Hardhat

```shell
  npm install
  npx hardhat compile
  npx hardhat run scripts/deploy.js --network rinkeby  
  npx hardhat verify <APIkey>  --network rinkeby  
```
